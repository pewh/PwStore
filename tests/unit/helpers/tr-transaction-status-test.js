import {
  trTransactionStatus
} from 'pw-store/helpers/tr-transaction-status';

module('TrTransactionStatusHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = trTransactionStatus(42);
  ok(result);
});
