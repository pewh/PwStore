import {
  formatCurrency
} from 'pw-store/helpers/format-currency';

module('FormatCurrencyHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = formatCurrency(42);
  ok(result);
});
