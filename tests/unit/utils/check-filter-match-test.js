import checkFilterMatch from 'pw-store/utils/filter-model';

module('checkFilterMatch');

// Replace this with your real tests.
test('it works', function() {
  var result = checkFilterMatch();
  ok(result);
});
