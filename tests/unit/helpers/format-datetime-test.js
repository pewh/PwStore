import {
  formatDatetime
} from 'pw-store/helpers/format-datetime';

module('FormatDatetimeHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = formatDatetime(42);
  ok(result);
});
